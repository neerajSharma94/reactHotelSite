import React from 'react'

function Title({ title }) {
    return (
        <section className="section-title">
            <h4>{title}</h4>
            <div></div>

        </section>
    )
}

export default Title
